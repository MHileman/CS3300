<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "MG Hileman - Portfolio" %>">


    <title><%= content_for?(:title) ? yield(:title) : "Hileman Portfolio" %></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>



  <body>
    <!-- Navigation Bar -->
    <div class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom box-shadow">
      <nav class="my-0 mr-md-auto mr-md-3">
        <%= link_to "Portfolio", projects_path, :class => 'p-2 text-dark'  %>
        <%= link_to "About Me", "/pages/about", :class => 'p-2 text-dark'  %>
        <%= link_to "Contact", "/pages/contact", :class => 'p-2 text-dark'  %>
      </nav>

      <nav class="my-2 my-md-0 mr-md-3">
        <div class="btn-group">
          <% if user_signed_in? %>
            <banner class="p-2 text-dark"> Welcome <strong><%= current_user.email %></strong>!</banner>
            <form><%= link_to 'Profile', edit_user_registration_path, :class => 'btn btn-outline-secondary' %></form>
            <%= button_to "Logout", destroy_user_session_path, method: :delete, :class => 'btn form-inline btn-outline-primary'  %>
          <% else %>
            <%= link_to "Login", new_user_session_path, :class => 'btn btn-outline-secondary'  %>
            <%= link_to "Sign up", new_user_registration_path, :class => 'btn btn-outline-primary'  %>
          <% end %>
        </div>
      </nav>
    </div>


    <% if notice %>
      <p class="alert alert-success"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p class="alert alert-danger"><%= alert %></p>
    <% end %>

    <%= yield %>
  </body>

  <footer class="text-muted bg-dark footer footer-dark fixed-bottom">
    <div class="container">
      <p class="float-right">
        <a href="#">Back to top</a>
      </p>
      <p class="text-center">This website was designed and created by Matthew Hileman.<br>
      If you would like to know about my past work, feel free to <%= link_to "conact me", "/pages/contact" %>.</p>
    </div>
  </footer>
</html>
